#include <iostream>

int main()
{
    setlocale(LC_ALL, "Russian");
    int* p; // Указатель на целочисленную переменную
    p = new int; // Выделяем память под целочисленную переменную
    *p = 100; // Записываем целое число по адресу
    std::cout << "По адресу " << p << " ";
    std::cout << "записано значение " << *p << std::endl;
    delete p; // Освобождаем память
    
    std::cout << "По адресу " << p << " ";
    std::cout << "записано значение " << *p << std::endl;
}
